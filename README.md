createMenu(title, id, dropDownArray)

Creates a menu. 

Will create a title for the menu.. append it to an element based on that element's id.
The array you put in the arguments will be used to create the menu.